
## デグレ防止：実投資額基準の想定利益

### 今回のデグレ原因
既存仕様として「個別購入を収益タブへ登録する場合、想定利益は実際に入力した投資額を基準にする」としていたにもかかわらず、フロントエンドから実投資額基準で計算した `planned_expected_profit` をバックエンドの入力スキーマへ追加する際の契約確認が不足していた。その結果、バックエンドで値を受け取れず、`create_manual` が投票プランの予定投資額を使って再計算する経路が残った。

### 検証上の原因
フロントエンドが正しい値を計算して送信することだけを確認し、API入力スキーマでその値が保持され、DB登録後の収益表示まで同じ値が到達することを一連の経路として回帰検証していなかった。

### 本質的な原因
フロントエンド・APIスキーマ・バックエンド登録処理のデータ契約を一体として検証せず、「送信側が正しい」ことだけで修正完了と判断したこと。

### 再発防止策
- 実投資額と想定投資額を必ず別フィールドとして扱う。
- `planned_expected_profit` のようなフロント計算値をAPIへ渡す場合、Pydantic等の入力スキーマに必ず定義する。
- 個別購入では `actual_stake` を想定利益計算の基準にする仕様を不変条件として扱う。
- 修正時は「画面表示 → API送信 → APIスキーマ → DB登録 → 収益一覧表示」まで同一データが維持されることを確認する。
- 「機能が動く」だけでなく、実投資額100円・予定投資額6000円のように値が異なる代表ケースを必ず回帰検証する。
- 既存仕様を変更しない修正では、既存仕様を壊していないことを確認できるまでcommit/pushしない。

## 最優先：デグレ防止・既存仕様保護

### 2026-09-14 デグレ発生原因と恒久対策

#### デグレを発生させた原因

- **開発上の原因**：既存仕様を維持すべき箇所について、変更前に既存仕様と変更履歴を十分に確認・固定せず、別の目的で導入された条件・実装を投票プランの候補選定処理へ混入させた。
- **検証上の原因**：修正対象の機能が動くことだけを確認し、既存仕様の代表ケースが修正後も維持されることを回帰検証する工程が不足していた。
- **設計上の原因**：同じ閾値に関係する設定がスキーマ、バックエンドの候補選定、フロントエンドの送信処理に分散しており、変更影響範囲を一括して確認しないと既存仕様を保証できない状態だった。
- **本質的な原因**：単一の値を誤設定したことではなく、**既存仕様を不変条件として保護せず、変更影響範囲と回帰確認を十分に管理しないまま修正を進めたこと**。

#### 恒久対策

1. **デグレ防止を最優先とし、機能追加・性能改善・最適化より優先する。**
2. 作業開始時に必ず `PROGRESS.md` の本章、関連する現行仕様、変更履歴を確認する。
3. 既存仕様を変更しない修正では、その仕様を「維持すべき不変条件」として明示する。
4. 閾値・候補除外・ランキング・購入対象などを変更するときは、スキーマ、バックエンド実処理、フロントエンド送信値の全経路を確認する。
5. 修正後は新機能の動作確認だけでなく、**既存の代表ケースが以前と同じ結果になることを必ず回帰検証する。**
6. 既存仕様を壊す可能性が残っている場合はcommit/pushせず、原因と影響範囲を再確認する。
7. 「仕様変更」と「バグ修正」を混同しない。既存仕様を変更する場合は、意図した仕様変更であることを明示してから実施する。
8. 今後の全作業で、**既存機能を壊さないことを最優先の判断基準とする。**

# Keirin-EV-Tool 進捗・引き継ぎメモ(最小版)

このファイルは、Claude・Grok・ChatGPT どれが作業する場合でも
「今どこまで進んでいて、次に何をすべきか」と「絶対に守るべき運用ルール」
だけを載せた最小版です。

**2026-09-07: 本体が肥大化してルールが守られなくなったため最小化した。**
プロジェクト概要・エンドポイント一覧・未解決の課題一覧・技術的教訓・過去の
詳細な検証ログは、すべて `PROGRESS_ARCHIVE.md` に移動済み。作業前に経緯や
仕様を確認したい場合はそちらを参照すること。

## 【Grok専用・必読・毎作業の最初に読む】

この節は **Grokが担当するときの作業契約**。一般ルールに加えて必ず守る。


### 事故記録 2026-09-16: 構文エラーのまま commit/push

- **原因**: Termuxスクリプトで `line_restricted` 削除時に `if two_param_mode` のインデントが崩れ `IndentationError`。`py_compile` は失敗したがシェルに `set -e` が無く、その後の `git commit` / `git push` が実行された。
- **対策（Grok必守）**:
  1. スクリプト先頭で `set -euo pipefail`
  2. `py_compile` 成功後でなければ `git add` しない
  3. `git commit` 前に再度 `python3 -m py_compile` を走らせ、失敗なら exit 1
  4. 「compile失敗なのに DONE と出る」出し方は禁止

### A. 成果物の渡し方（最重要）

1. **サンドボックス上の編集だけではユーザー環境（Termuxのクローン）に届かない。**
2. 修正は必ず **Termuxに貼って完了する一続きのスクリプト**で出す
   （ファイル書き換え → py_compile → git add/commit/push → 確認curl）。
3. 「sandboxで直したので取り込んで」だけは **不完了**。`nothing to commit` になる出し方は禁止。

### B. 三連単の「予想勝率」定義（2026-09-16確定・不変）

1. **1投票 = 1買い目の的中確率**（その組み合わせのHarville確率）。
2. **1着勝率を三連単の勝率として保存・表示・集計してはならない。**
3. 三連単は典型的に **数%前後**。30〜40%は異常値。
4. **禁止**: 2着/3着を「同ライン残のみ」に候補限定すること
   （残り1頭で条件付き確率≈1 → 三連単確率が1着勝率まで膨らむ。購入履歴39%の根因）。
5. `SAME_LINE_REMAIN_*` は常に False・再有効化禁止。危険分岐はコードから削除する。
6. 汚染された `win_prob_at_purchase` を使う集計は、再計算/backfill まで信用しない。

### C. 作業開始時

1. 本節と「1. 現在の状況」を読む。
2. 修正は Termux スクリプト付きで出す。
3. デプロイ後の確認 curl を付ける。

---

## 【運用ルール・必読・厳守】

**このファイルは「追記」ではなく「上書き」が原則。** 積み増すと肥大化し、
誰も全部読まなくなって引き継ぎが機能しなくなる(2026-09-05/09-07に発覚)。

1. 「1. 現在の状況」は**常に最新1件だけ**を残す。前の内容は消してよい。
2. このファイルに日付ごとの詳細な実験結果・数値の羅列・未解決の課題一覧を
   書き足さない。すべて `PROGRESS_ARCHIVE.md` に書く(そちらは追記可・肥大化OK)。
3. 作業を終えるとき・区切りがついたときは、必ず「1. 現在の状況」を
   その時点の内容に**書き換える**。「最終更新」の日付と担当も書き換える。

---

## 1. 現在の状況

最終更新: 2026-09-16(Grok) — threshold-policy-scan（見送り込み閾値政策集計）追加。文言は的中率。

## 1. 現在の状況_OLD_PLACEHOLDER(★これだけ読めば足りる・毎回上書きする)

最終更新: 2026-09-16(Grok) — 三連単勝率過大（同ライン残限定）の修正適用

### 直近
- 購入履歴の三連単予想勝率39%等は、同ライン残限定Harvilleが原因。
- 危険分岐削除 + 履歴は再計算表示 + backfill API。
- Grokは sandbox のみの修正を禁止（Termuxスクリプト必須）。

### 次
1. 履歴で再計算%が数%台か確認
2. backfill で保存値を書き戻し
3. 勝率帯集計をやり直し


### 目的・評価（厳守）
1. **高配当依存は許容**。ただし低配当帯でも結果が出ているかを見る
2. **金額より的中率の精度一致**を目指す（オッズのブレが激しい）
3. 想定的中率と実的中率のギャップ縮小が最優先
4. 想定は実績より上振れ → 入場の想定EV下限は高め（目安300%）

### 検証から確定した傾向
- 同ライン絡み ＞ 異ライン
- 先行有利バンク・逃げ/先行寄りが強い（濃厚）
- 勝率帯30%以上のズレは帯の切り方の問題。帯別校正に頼らず全体をならす

### 投票プラン（現行）
- prefer_hit_rate=True：三連単・1着＝本命
- prefer_same_line=True：同ライン絡みを優先（異ラインのみ除外）
- 並べ替え：的中確率 → オッズ
- オッズ上限なし／下限のみ、想定EV入場下限あり

### 的中率向上の課題
- [x] 1着本命 / 2着3着同ライン残 / プラン同ライン優先
- [ ] 逃げ・先行脚質の反映
- [ ] 全体の的中率ギャップ監視
- [ ] 低配当帯の的中有無の確認

---

## 0. 最優先作業ルール（必須・例外なし）

- **「進め」「続行」「全部進め」等の指示を受けた場合、途中で作業を止めない。**
  次にユーザーによる実行・認証・判断が必要になる地点まで、可能な作業を自律的に連続して進める。
- 「確認しました」「次は〜です」「ここで待ちます」等の中間報告だけで作業を終了してはならない。
- 原則として、
  **現行ソース確認 → 原因特定 → 最小修正 → 検証 → 診断実行 → 結果分析 → 必要な追加修正 → 再検証**
  の順に、実行可能なところまで連続して進める。
- 次に行う作業が明確な場合、ユーザーへの不要な確認質問を挟まない。
- ユーザーが実行・認証・判断しなければ進められない地点に到達した場合のみ、そこで停止して必要な操作を明示する。
- **PROGRESS.mdをKeirin-EV-Toolの作業ルールの正本とする。**
- **「進め」「続行」「全部進め」等を受けた作業では、作業開始時に必ずこの「0. 最優先作業ルール」を確認し、ユーザーの実行・認証・判断が必要になるまで自律的に連続実行する。**
  - 途中の調査・分析・修正・検証が完了しただけでは停止しない。
  - 「次に○○します」「次は○○です」「確認しました」「ここで待ちます」等を中間地点で終了条件にしない。
  - 次工程が明確なら、そのまま次工程へ進む。
  - ユーザーへコマンドを提示して結果待ちになる地点だけを停止地点とする。
  - 次のユーザー入力として再度「進め」を要求してはならない。
 会話上の一時的な指示よりも、この章に記載された必須ルールを優先して継続的に適用する。
- **ユーザーに提示する実行コマンドへ `git diff`、`git diff --check`、`git diff ...` 等の差分表示コマンドを絶対に含めない。** 差分確認が必要な場合は作業側で確認し、ユーザーには検証・commit・push等の必要なコマンドだけを提示する。
- 作業の区切り・完了時には、必ずPROGRESS.mdの「1. 現在の状況」をその時点の最新状態へ更新する。

---

## 0.1 大量・長時間処理の必須ルール（Render無料枠・厳守）

**最初から分割する。一括で長時間HTTPを叩かない。**

1. Render 無料のWebサービスは長時間リクエストを切る（502/504/空レスポンス）。
   目安: **1リクエスト90秒以内**に必ず終わる設計にする。
2. DB同期・全件replay・全表スキャン・大批量インポートは必ず次のいずれかにする。
   - **ステップ分割**（step=races / entries / odds …）
   - **件数制限**（limit + after_id / offset で続きから再開）
   - **クライアント側ループ**（シェルが短リクエストを繰り返す）
3. 「動くから一発」は禁止。タイムアウト後に分割するのは手戻りなので、
   **新規の管理API・バッチは設計時点で分割済み**にする。
4. ユーザー向けコマンドも「1本で全部」より、
   **途中再開可能なスクリプト**（続きから / 残り0まで）を標準にする。

---

## 5. 運用ルール・やってはいけないこと

- **Harville本体(`combination_prob`/`wide_prob`/`harville_prob`)を推測で
  全面的に書き換えない。** 変更する場合は実データで検証してから。
- **`is_complete`フラグや係数を手動でTrue/改ざんしない。**
- **負のEVを積極的に買う方向への変更はしない**(既に再計算で負は正当と確認済み)。
- **選択ロジックの閾値(EV最大化 vs 的中率重視など)を勝手に変えない。**
  設計合意してから。
- git操作(push)はユーザーが実行する。Claude/Grokはzipまたはパッチで返す。
- **git commitとgit pushのコマンドは、実行状態を考慮して書くこと。**
  - `git commit`が既に成功している場合、次のコマンドは`git push origin main`だけを書く。
  - `git commit && git push`を、既にコミット済みの状態で再実行しない。
    `git commit`が「nothing to commit」で終了コード1になると`&&`により`git push`まで実行されないため。
  - 修正→commit→pushを一度に提示する場合は、commit対象を明示し、commit成功後にpushまで実行される構成にする。
  - ユーザーがcommit済みの実行結果を提示した場合は、commitを再実行せず、pushだけを提示する。
- 既存実装を確認した上で最小差分。推測での大規模改修はしない。
- **投票ロジック(予想確率・確率補正・運用ゲート等)を修正したら、
  `app/routers/purchases.py`の`CALIBRATION_SWITCH_AT`を必ずその日の日付に
  更新すること。** 集計・診断系エンドポイントは既定でこの日時以降だけを
  「現行基準」として集計するため、更新を忘れると新旧ロジック混在データが
  現行基準として表示されてしまう(2026-09-06にのんの指摘で発覚)。
- デプロイコマンドは実際に動く形で書く(プレースホルダー禁止)。
  リポジトリのローカルパスは `~/Keirin-EV-Tool`、zipのダウンロード先は
  `~/storage/downloads`。
  型:
  ```bash
  cd ~/Keirin-EV-Tool && \
  unzip -o ~/storage/downloads/<zip名> -d . && \
  git add <変更ファイル> && \
  git commit -m "<内容>" && \
  git push && \
  rm -f ~/storage/downloads/<今回のzip>
  ```
- **`git add -A` / `git add .` は絶対に使わない。** 必ず今回変更したファイルだけを
  明示的に列挙して`git add`する。
- **commit対象を提示する前に、必ず`git status --short`の出力を1行ずつ確認する。**
  以下に該当する行が1つでもあれば、その行は`git add`の対象から除外する(気づいた場合は
  ユーザーに一言注意喚起する)。
  - `__pycache__/`、`*.pyc`
  - `*.bak`、`*.bak_*`などのバックアップファイル
  - `scraper/data/`配下のログ・replay・joblist等の実行時生成物
  - `160000`表示や「embedded git repository」警告が出るサブディレクトリ(埋め込みリポジトリ)
  - その他、今回の修正と無関係なファイル
  (2026-09-07: `git add -A`を提示した結果、上記が全て誤ってコミットされる事故が発生したため
  ルール化)。
- コマンドはできるだけ1行・スクリプト化。bankrollは検証時1000000を明示。
- 実資金投票は精度が目安に届くまで開始しない
  (investment-readinessの4基準: サンプル数・統計的有意性・実績収支率黒字・
  破産確率10%以下)。
- スクリプト実行コマンド(replay_settled.py等)を提示する際は、必ず
  `cd ~/Keirin-EV-Tool &&` を先頭に明記する(単体で書くとFileNotFoundになる)。
- 重複していた「追加分析課題」一覧(2系統)・2026-09-09のChatGPT引き継ぎメモは
  `PROGRESS_ARCHIVE.md`末尾に退避済み(2026-09-10整理)。過去の経緯を確認したい
  場合はそちらを参照する。現在の課題は上記「1. 現在の状況」の「残作業」のみ。

## since時刻運用ルール（2026-09-13）
- `since` の基準時刻は必ず**秒単位のUTC絶対時刻**で記録する。
- 暦日だけ、JST 00:00、日付から推測した時刻を `since` の基準にしない。
- `VOTING_CRITERIA_UPDATED_AT` は最後に投票判断そのものを変更した時刻を記録する。
- `since=calibration_switch` は `VOTING_CRITERIA_UPDATED_AT` を使用する。
- タイムゾーン付きISO日時はUTCへ正規化してから比較する。
- 過去レースの再投票では `Purchase.purchased_at` は再投票実行時刻になるため、レース開催日時と混同しない。
- 今回の投票基準変更の記録時刻は、変更コミット `a592f0977a9bae1aad152b67bc8c015cf5beda8b` のコミット時刻
  `2026-09-12 15:21:47 UTC`（JST `2026-09-13 00:21:47`）を基準とする。
- 今回の9/12未確定レースは既に結果確定済みであり、その後の全過去レース再投票結果を現行モデルの検証対象とする。
- 9/12のうち今回の未確定39レースに含まれない既確定レースだけを、再投票漏れ候補として別途確認する。
## デグレ防止：収益記録の想定利益計算

- **発生原因**：実投資額と予定投資額を分離して管理する仕様であるにもかかわらず、修正前に実際の値の取得元・計算元・API送信値・バックエンド保存処理まで一連の経路を確認せず、既存仕様を満たしているフロント側の計算とは別に、バックエンドの再計算処理だけを前提として原因を判断した。
- **本質的な原因**：値の取得元から保存先までのデータフローを確認せずに修正箇所を決めたこと。実投資額(`actual_stake`)と予定投資額(`planned_stake`)という異なる意味の値について、どの処理が最終値を決定するかを変更前に固定していなかった。
- **対策**：今後の修正では、必ず「入力値の取得元 → 計算元 → API payload → バックエンド受信 → DB保存 → 表示」の実経路を先に確認し、その経路上で最終値を決定している箇所を特定してから修正する。フロントとバックエンドの双方に同じ値の計算が存在する場合は、サーバー側で仕様上の不変条件を保証する。
- **今回の不変条件**：`planned_stake` は投票プラン上の予定投資額、`actual_stake` は実際の投資額。`planned_expected_profit` は `actual_stake × (planned_win_prob × planned_odds − 1)` とする。
- **回帰ケース**：予定投資6000円、実投資100円、的中率3.4%、オッズ100倍の場合、想定利益は14,400円ではなく240円でなければならない。

## デグレ防止：検証完了前のコミット・プッシュ禁止

- **作業原因**：検証工程とコミット・プッシュ工程を明確に分離せず、検証が成功した場合だけ後続工程へ進むという作業フローを保証していなかった。
- **対策**：今後は「検証成功」をコミット・プッシュの必須前提条件とする。検証が1つでも失敗した場合は、`git add`・`commit`・`push` を実行せず、その時点で作業を停止する。
- **運用ルール**：検証 → 成功確認 → `git add` → `commit` → `push` → push後確認の順序を固定し、検証未完了の変更を確定・公開しない。
- **最優先事項**：デグレ防止と同様、検証工程を省略・迂回せず、検証成功をコミット・プッシュの絶対条件とする。

## デグレ防止：作業を途中で停止しない運用

- **作業原因**：修正・検証・確定・公開を一連の作業として最後まで完了させるという運用を徹底せず、途中工程で作業が停止する状態を許容した。
- **対策**：「修正して」「進めて」と指定された作業では、現状確認 → 原因特定 → 最小修正 → 検証 → commit → push → push後確認までを連続して実施し、次にユーザーの操作・判断が必要になる地点まで途中で停止しない。
- **停止条件**：対象ソースが想定と一致しない、検証対象が特定できない、または修正によるデグレの可能性が解消できない場合のみ、commit・pushを行わず停止する。
- **検証方法**：数値計算の回帰検証では浮動小数点の完全一致比較を避け、仕様上の許容誤差を用いて判定する。

### 作業継続・改行混入防止
- Keirin-EV-Toolの作業開始時は必ずPROGRESS.mdの最優先作業ルールを確認する。
- 「進め」「続行」「修正して」等の指示では、ユーザーの操作・認証・判断が必要になる地点まで途中で停止しない。
- 現状確認→原因特定→最小修正→構文検証→回帰検証→commit→push→push後確認を一連の作業として扱う。
- 長大な多段shellコマンドや複数heredocの連結による貼り付け時の改行・空白混入を避ける。今後は原則として1つの実行ブロックにまとめ、対象文字列の完全一致だけに依存しない。
- 修正前に取得元・計算元・送信元・受信側・保存先を確認し、修正対象外の未コミット変更を巻き込まない。


## Termux実行前提
- Keirin-EV-Toolの実作業はユーザーがTermuxでコマンドを実行する前提とする。
- ChatGPT側でリポジトリやDBを直接操作した前提で進めず、Termuxで実行可能なコマンドを提示する。
- 既存データを修正する作業では、コード修正だけで完了扱いにせず、既存DBデータの補正とAPI/DBによる実データ確認まで完了させる。

## Termux依存環境の確認ルール
- Keirin-EV-Toolの実作業はユーザーがTermuxで実行する。
- TermuxのシステムPythonに本番依存ライブラリが導入済みとは仮定しない。
- アプリ内部モジュールをimportしてDB/APIを直接操作する前に、必ず`requirements.txt`と実行環境の依存関係を確認する。
- FastAPI等の依存ライブラリがTermuxに存在しない場合、勝手にシステムPythonへインストールせず、Render APIまたはプロジェクトが実際に使用している実行経路を優先する。
- 「確認→実行→検証」では、実行環境の前提条件を確認してから実行する。

## 収益記録 planned_expected_profit 永続化整合性
- `planned_expected_profit` は予定投資額(`planned_stake`)ではなく、実投資額(`actual_stake`)を基準にする。
- `PUT /revenue/{live_bet_id}` の `update_live_bet()` で `actual_stake` が変更された場合、DB上の`planned_expected_profit`も同時に再計算して永続化する。
- `_row_to_dict()`だけで表示値を補正しても既存DB値・stats/equityの集計値は古いままになるため、DB値自体を補正する。
- 既存データの補正は、修正後API経由で対象行を更新し、更新前後のAPI値を確認して完了とする。

## 収益タブ：実投資額基準の統一

- 原因: 収益タブの想定集計が投票計画上の投資額を基準にしていたため、実投資額を変更した場合に想定利益と想定回収率の基準が不一致になる構造だった。
- 設計方針: 収益タブの投資額は実投資額のみを正とする。投票計画上の投資額は収益集計・比較・資産推移には使用しない。
- 実装: `revenue_stats()`、`equity_curve()`、`_row_to_dict()` を実投資額基準へ統一。
- UI: 「想定投資額」および「実績−想定」の投資額差を表示しない。
- 回帰条件: 実投資額100円、確率3.4%、オッズ100倍なら想定利益240円、想定回収率340.00%。
- 再発防止: 収益系処理では投資額の取得元からAPIレスポンス・UI表示まで実投資額基準で追跡する。
- 既存未コミット変更: `app/schemas.py` は対象外として保護する。
- ルール: 修正・検証・commit・push・デプロイ後API確認まで止めずに一連で実施し、ユーザーの実行が必要になる地点まで進める。


## 最優先作業ルール：途中で止まらない

- 「止まらずに直して」「進め」「続行」「修正して」と指示された場合、確認だけで停止しない。
- ソース確認→原因特定→最小修正→構文・回帰検証→commit→push→デプロイ後API確認まで、ユーザーの追加操作が必要になる地点まで一連で進める。
- 途中で検証エラーが発生した場合も、原因が安全に特定できる範囲ではその場で修正して続行する。


## 収益タブ：実投資額基準への統一 完了確認（2026-09-16）

- `/revenue/stats` の想定利益・想定回収率は `actual_stake_sum` を基準として統一。
- 実投資額100円、期待利益240円の場合、想定回収率340%をRender APIで確認。
- 収益タブから「想定投資額」の表示を削除。
- 収益タブの実績投資額は `actual_stake` のみを使用。
- 「1レース上限%」は資金管理シミュレーション機能の項目であり、収益タブの旧表示とは区別して維持する。

### 収益タブ資産推移の想定系列復旧（2026-09-15）
- 原因: 収益タブの投資額基準を `actual_stake` に統一した際、`/revenue/equity-curve` の `planned` 系列まで削除され、フロント側に想定線を渡さなくなっていた。
- 修正: 想定系列を復旧。ただし投資額・横軸は `actual_stake` のみを使用し、`planned_stake` は使用しない。想定損益は `actual_stake * (planned_win_prob * planned_odds - 1)` を累積する。
- フロント側は既存の `data.planned` 描画処理を使用するため変更不要。
- 再発防止: 「想定投資額を収益タブから除去すること」と「資産推移の想定系列を除去すること」を混同しない。統計・一覧・グラフの各投資額基準を個別に検証する。

## フロント全操作不能（タブ遷移・本日レース表示・全ボタン無反応）の修正（2026-09-16）

- 症状: index.htmlを開いてもタブが切り替わらない、本日のレースが表示されない、どのボタンを押しても反応しない。
- 原因: `js/app.js` 内に `if (false) { ... }` で無効化されていた閾値政策集計の旧処理（2162〜2205行付近、コメント「replaced by thresholdPolicyPanel handlers」）が残存しており、その中に非同期関数の外で `await` を使用した箇所があった。`js/app.js` は `<script>`（非module）としてブラウザに読み込まれるため、この記法はJavaScriptの構文エラーとなり、ファイル全体が一切実行されない状態になっていた。
- 結果として `DOMContentLoaded` 以降の初期化処理（本日レース取得・タブ切り替えイベント・全ボタンのイベントリスナー登録）が一つも走らず、画面上のあらゆる操作が無反応になっていた。
- 修正: 到達不能な `if (false) { ... }` ブロック（閾値政策集計の旧処理一式、44行）を丸ごと削除。同機能は既存の `loadThresholdPolicyScan()` に統合済みのため実害なし。
- 検証: `node -c js/app.js` で構文エラーが解消されたことを確認。
- 再発防止: 無効化コードを `if (false) {}` で残す場合でも、非module scriptでは `await` を含む記法をそのまま残さない。不要になった処理は `if(false)` で残さず削除する。

## 新設ボタン(閾値政策集計パネル)が反応しない問題の追加修正（2026-09-16 その2）

- 症状: 前回の構文エラー修正をgit pull・反映後も、検証タブの新設5ボタン(閾値政策集計パネル)が反応しない/消えたように見える。
- 原因: `sw.js`（Service Worker）はキャッシュ優先方式で `js/app.js` を配信しており、`CACHE_NAME` を更新しない限りブラウザは修正前の(構文エラーを含む)古い `app.js` をキャッシュから返し続ける。前回の修正時に `CACHE_NAME` の更新を失念していたため、GitHubへのpushとRenderへの反映は完了していても、ブラウザ側は旧キャッシュのまま構文エラー入りのapp.jsを実行し続けていた。
- 修正: `sw.js` の `CACHE_NAME` を `keirin-ev-v104` → `keirin-ev-v105` に更新。あわせて `index.html` の `<script src="js/app.js?v=100">` を `?v=101` に更新し、二重にキャッシュ破棄を確実化。
- 再発防止: フロントエンド(`index.html` / `js/app.js` / `css/style.css`)を修正した際は、`sw.js` の `CACHE_NAME` を必ず同時に更新すること（`sw.js` 冒頭のコメントにも明記済みの既存ルール）。

## 閾値政策集計パネルの表示バグ（テンプレート文字列破損）修正（2026-09-16 その3）

- 症状: ボタンを押すと結果は表示されるが、`(  {fmt(d.scope)}` のようにコード片がそのまま文字として表示され、値が計算されない。
- 原因: `loadThresholdPolicyScan()` 内のテンプレートリテラルで、本来連続する2つの `${...}` 埋め込みのうち、前半の `${` が `\( ` に、後半の `}` が ` \)` に破損しており、埋め込み式が評価されずそのまま文字列として出力されていた（Grok側での編集時に紛れ込んだ破損と推定）。該当箇所は17箇所。
- 修正: 破損していた `\( {` と ` \){` を、本来の `${` に一括で復元。`node -c` で構文エラーがないことを確認済みです。
- あわせて `sw.js` の `CACHE_NAME` を `v105`→`v106`、`index.html` の `app.js?v=101`→`?v=102` に更新し、ブラウザキャッシュを確実に破棄。

## 閾値政策集計パネルの調整（帯の細分化・不要項目削除・上位K拡張）（2026-09-16 その4）

- 予想的中率帯: 0〜2%の範囲を0.1%刻み(20帯)に細分化。2%以上は従来通り(2-3%, 3-5%, 5-8%, 8-10%, 10-15%, 15%+)。
- オッズ帯: 0〜30倍の範囲を1倍刻み(30帯)に細分化。30倍以上は従来通り(30-50, 50-100, 100-300, 300-1000, 1000+)。
- 上位K件シミュレーションから「レース的中率%」列を削除(バックエンド`topk_sim`・フロント`tableFromArr`の両方)。
- 上位K件シミュレーションの対象kを (1,2,3,5,8) から (1,2,3,5,8,10,15,20) に拡張。
- EV帯に負の値が出る件: バグではなく仕様通り。EV(期待値%) = (予想的中率×オッズ－1)×100 は数式上マイナスになり得る。特に「見送り」データは、そもそもEVが低い/マイナスだったために見送られた候補が多く含まれるため、マイナスのEV帯が出現するのは正常な挙動。scope=purchase(実投票のみ)に絞った際にもマイナスEVの実投票が含まれている場合は、購入時の閾値ゲートとの整合性を別途確認する必要がある。
- 的中率が高いほどROIが上がらない件: ROI% ≈ 実績的中率 × 平均オッズ の関係にあり、予想的中率が高い帯ほど一般に平均オッズは低くなる(市場のインプライド確率が高い=オッズが低い)ため、的中率が高い帯でも払戻倍率が小さく、ROIが比例して伸びるとは限らない。ROIを予想的中率と連動させたい場合は、市場インプライド確率に対して予想的中率がどれだけ上回っているか(＝EVそのもの)で評価する方が本来の目的に合致する。なお、1000〜3000倍オッズ帯での予想的中率と実績の乖離(既知の未解決課題、乖離率5.78倍 n=495)がある場合、高オッズ帯のROIが歪み、低的中率帯のROIが見かけ上高くなる一因になり得る。実データでの帯別「平均オッズ」列と付き合わせて確認することを推奨。

## 投票プラン閾値の確定反映・オッズ帯変更・上位K件をEV基準に変更（2026-09-16 その5）

- 最低勝率フィルター(minProbInput)のデフォルトを 10% → 2% に変更(投票プランの下限を2%で確定)。
- 安全マージン(minEvInput、EV下限)のデフォルトを 50% → 0% に変更(EV下限なしで確定。ただしEV下限判定自体はサーバー側で0%未満を除外する基本フロア`max(min_ev_pct, 0.0)`が別途複数箇所に存在し、そこは今回変更していない。安全マージンの上乗せ分のみ0にした)。
- **重要**: 上記2つはこの端末のブラウザのlocalStorageに保存された既存値が優先されるため、HTMLのデフォルト値を変えただけでは既存端末には反映されない。設定タブで「最低勝率フィルター」を2、「安全マージン」を0に手動で書き換えて「設定を保存」を押す必要がある。
- オッズ帯: 0〜30の1倍刻み細分化を廃止し「<30」の単一帯に戻した。かわりに30〜100を1倍刻みで細分化。100以上は従来通り(100-300, 300-1000, 1000+)。
- 上位K件シミュレーション(4_1レース上位K件)のレース内ランキング基準を「予想的中率降順」から「EV降順」に変更。EV%=(予想的中率×オッズ－1)×100 であり、予想的中率×オッズがそのまま回収期待倍率、そこから元本分の1を引いてパーセント化したものがEV。ユーザーの「予想的中率×オッズ＝EV」という理解は近いが、正確には「予想的中率×オッズ－1」が回収期待の増分(元本超過分)であり、それを%表示したものがEV%。既存の各行データには`ev`フィールドとして算出済みのため、そのままランキングキーに使用した。

## オッズ下限スイープの細分化・投票プランのEVソート化（2026-09-16 その6）

- オッズ下限スイープ(2_下限スイープ_オッズ)を帯と同じ粒度に変更。閾値を [0,30,35,50,100,300] から [0, 30,31,...,100(1刻み), 150,200,300,500,1000] に変更。
- **投票プラン(race_plan/実際の購入候補選定)のソート順を「予想的中率降順→オッズ降順」から「EV降順→予想的中率降順」に変更。** これは実際の購入候補(現行はmax_items=1の「戦略A」運用のため、1位の選び方が変わると実際に買う銘柄そのものが変わる)に直結する変更。
  - 注意: 2026-09-13付けのんの承認により「全期間データ検証の結果、最高勝率1点選択が複数点分散より実績で優れていた(single-bet-strategy-compare診断)」という理由でこの勝率優先ソートが採用された経緯がある。今回の変更で従来の検証結果と矛盾する形になるため、今回のEVソート化後の実績は改めて検証が必要。
