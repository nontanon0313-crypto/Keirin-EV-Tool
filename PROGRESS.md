
## デグレ防止：実投資額基準の想定利益

### 今回のデグレ原因
既存仕様として「個別購入を収益タブへ登録する場合、想定利益は実際に入力した投資額を基準にする」としていたにもかかわらず、フロントエンドから実投資額基準で計算した `planned_expected_profit` をバックエンドの入力スキーマへ追加する際の契約確認が不足していた。その結果、バックエンドで値を受け取れず、`create_manual` が投票プランの予定投資額を使って再計算する経路が残った。

### 検証上の原因
フロントエンドが正しい値を計算して送信することだけを確認し、API入力スキーマでその値が保持され、DB登録後の収益表示まで同じ値が到達することを一連の経路として回帰検証していなかった。

### 本質的な原因
フロントエンド・APIスキーマ・バックエンド登録処理のデータ契約を一体として検証せず、「送信側が正しい」ことだけで修正完了と判断したこと。

### 再発防止策
- 実投資額と想定投資額を必ず別フィールドとして扱う。
- `planned_expected_profit` のようなフロント計算値をAPIへ渡す場合、Pydantic等の入力スキーマに必ず定義する。
- 個別購入では `actual_stake` を想定利益計算の基準にする仕様を不変条件として扱う。
- 修正時は「画面表示 → API送信 → APIスキーマ → DB登録 → 収益一覧表示」まで同一データが維持されることを確認する。
- 「機能が動く」だけでなく、実投資額100円・予定投資額6000円のように値が異なる代表ケースを必ず回帰検証する。
- 既存仕様を変更しない修正では、既存仕様を壊していないことを確認できるまでcommit/pushしない。

## 最優先：デグレ防止・既存仕様保護

### 2026-09-14 デグレ発生原因と恒久対策

#### デグレを発生させた原因

- **開発上の原因**：既存仕様を維持すべき箇所について、変更前に既存仕様と変更履歴を十分に確認・固定せず、別の目的で導入された条件・実装を投票プランの候補選定処理へ混入させた。
- **検証上の原因**：修正対象の機能が動くことだけを確認し、既存仕様の代表ケースが修正後も維持されることを回帰検証する工程が不足していた。
- **設計上の原因**：同じ閾値に関係する設定がスキーマ、バックエンドの候補選定、フロントエンドの送信処理に分散しており、変更影響範囲を一括して確認しないと既存仕様を保証できない状態だった。
- **本質的な原因**：単一の値を誤設定したことではなく、**既存仕様を不変条件として保護せず、変更影響範囲と回帰確認を十分に管理しないまま修正を進めたこと**。

#### 恒久対策

1. **デグレ防止を最優先とし、機能追加・性能改善・最適化より優先する。**
2. 作業開始時に必ず `PROGRESS.md` の本章、関連する現行仕様、変更履歴を確認する。
3. 既存仕様を変更しない修正では、その仕様を「維持すべき不変条件」として明示する。
4. 閾値・候補除外・ランキング・購入対象などを変更するときは、スキーマ、バックエンド実処理、フロントエンド送信値の全経路を確認する。
5. 修正後は新機能の動作確認だけでなく、**既存の代表ケースが以前と同じ結果になることを必ず回帰検証する。**
6. 既存仕様を壊す可能性が残っている場合はcommit/pushせず、原因と影響範囲を再確認する。
7. 「仕様変更」と「バグ修正」を混同しない。既存仕様を変更する場合は、意図した仕様変更であることを明示してから実施する。
8. 今後の全作業で、**既存機能を壊さないことを最優先の判断基準とする。**

# Keirin-EV-Tool 進捗・引き継ぎメモ(最小版)

このファイルは、Claude・Grok・ChatGPT どれが作業する場合でも
「今どこまで進んでいて、次に何をすべきか」と「絶対に守るべき運用ルール」
だけを載せた最小版です。

**2026-09-07: 本体が肥大化してルールが守られなくなったため最小化した。**
プロジェクト概要・エンドポイント一覧・未解決の課題一覧・技術的教訓・過去の
詳細な検証ログは、すべて `PROGRESS_ARCHIVE.md` に移動済み。作業前に経緯や
仕様を確認したい場合はそちらを参照すること。

## 【運用ルール・必読・厳守】

**このファイルは「追記」ではなく「上書き」が原則。** 積み増すと肥大化し、
誰も全部読まなくなって引き継ぎが機能しなくなる(2026-09-05/09-07に発覚)。

1. 「1. 現在の状況」は**常に最新1件だけ**を残す。前の内容は消してよい。
2. このファイルに日付ごとの詳細な実験結果・数値の羅列・未解決の課題一覧を
   書き足さない。すべて `PROGRESS_ARCHIVE.md` に書く(そちらは追記可・肥大化OK)。
3. 作業を終えるとき・区切りがついたときは、必ず「1. 現在の状況」を
   その時点の内容に**書き換える**。「最終更新」の日付と担当も書き換える。

---

## 1. 現在の状況(★これだけ読めば足りる・毎回上書きする)

最終更新: 2026-09-13(Claude) — 1点賭け戦略Aを本番投票プランに実装

### 投票プラン（現行・厳守）
- **1レース1点のみ投票(戦略A採用)**。全期間データでの検証(single-bet-strategy-compare)で
  「最高勝率1点」(31的中/368件・ROI1419%)の方が「最高EV1点」(5的中/368件・ROI4456%)より
  母数が多く再現性の信頼度が高いと判断(のん承認2026-09-13)
- `max_items`既定値を8→1に変更。1点のみの場合はレース予算全額をその1点に投じる
  (`max_single_bet_pct_of_race_cap`既定値も0.4→1.0に変更)
- prefer_hit_rate=True：三連単・1着＝本命、同ライン絡み優先、並べ替えは的中確率→オッズ
- 戦略B(最高EV)の上位的中除外感度分析を追加(`single-bet-strategy-compare`に統合)

### 目的・評価（厳守）
1. **高配当依存は許容**。ただし低配当帯でも結果が出ているかを見る
2. **金額より的中率の精度一致**を目指す（オッズのブレが激しい）
3. 想定的中率と実的中率のギャップ縮小が最優先
4. 想定は実績より上振れ → 入場の想定EV下限は高め（目安300%）

### 検証から確定した傾向
- 同ライン絡み ＞ 異ライン
- 先行有利バンク・逃げ/先行寄りが強い（濃厚）
- 勝率帯30%以上のズレは帯の切り方の問題。帯別校正に頼らず全体をならす

### 投票プラン（現行）
- prefer_hit_rate=True：三連単・1着＝本命
- prefer_same_line=True：同ライン絡みを優先（異ラインのみ除外）
- 並べ替え：的中確率 → オッズ
- オッズ上限なし／下限のみ、想定EV入場下限あり

### 的中率向上の課題
- [x] 1着本命 / 2着3着同ライン残 / プラン同ライン優先
- [ ] 逃げ・先行脚質の反映
- [ ] 全体の的中率ギャップ監視
- [ ] 低配当帯の的中有無の確認

---

## 0. 最優先作業ルール（必須・例外なし）

- **「進め」「続行」「全部進め」等の指示を受けた場合、途中で作業を止めない。**
  次にユーザーによる実行・認証・判断が必要になる地点まで、可能な作業を自律的に連続して進める。
- 「確認しました」「次は〜です」「ここで待ちます」等の中間報告だけで作業を終了してはならない。
- 原則として、
  **現行ソース確認 → 原因特定 → 最小修正 → 検証 → 診断実行 → 結果分析 → 必要な追加修正 → 再検証**
  の順に、実行可能なところまで連続して進める。
- 次に行う作業が明確な場合、ユーザーへの不要な確認質問を挟まない。
- ユーザーが実行・認証・判断しなければ進められない地点に到達した場合のみ、そこで停止して必要な操作を明示する。
- **PROGRESS.mdをKeirin-EV-Toolの作業ルールの正本とする。**
- **「進め」「続行」「全部進め」等を受けた作業では、作業開始時に必ずこの「0. 最優先作業ルール」を確認し、ユーザーの実行・認証・判断が必要になるまで自律的に連続実行する。**
  - 途中の調査・分析・修正・検証が完了しただけでは停止しない。
  - 「次に○○します」「次は○○です」「確認しました」「ここで待ちます」等を中間地点で終了条件にしない。
  - 次工程が明確なら、そのまま次工程へ進む。
  - ユーザーへコマンドを提示して結果待ちになる地点だけを停止地点とする。
  - 次のユーザー入力として再度「進め」を要求してはならない。
 会話上の一時的な指示よりも、この章に記載された必須ルールを優先して継続的に適用する。
- **ユーザーに提示する実行コマンドへ `git diff`、`git diff --check`、`git diff ...` 等の差分表示コマンドを絶対に含めない。** 差分確認が必要な場合は作業側で確認し、ユーザーには検証・commit・push等の必要なコマンドだけを提示する。
- 作業の区切り・完了時には、必ずPROGRESS.mdの「1. 現在の状況」をその時点の最新状態へ更新する。

---

## 0.1 大量・長時間処理の必須ルール（Render無料枠・厳守）

**最初から分割する。一括で長時間HTTPを叩かない。**

1. Render 無料のWebサービスは長時間リクエストを切る（502/504/空レスポンス）。
   目安: **1リクエスト90秒以内**に必ず終わる設計にする。
2. DB同期・全件replay・全表スキャン・大批量インポートは必ず次のいずれかにする。
   - **ステップ分割**（step=races / entries / odds …）
   - **件数制限**（limit + after_id / offset で続きから再開）
   - **クライアント側ループ**（シェルが短リクエストを繰り返す）
3. 「動くから一発」は禁止。タイムアウト後に分割するのは手戻りなので、
   **新規の管理API・バッチは設計時点で分割済み**にする。
4. ユーザー向けコマンドも「1本で全部」より、
   **途中再開可能なスクリプト**（続きから / 残り0まで）を標準にする。

---

## 5. 運用ルール・やってはいけないこと

- **Harville本体(`combination_prob`/`wide_prob`/`harville_prob`)を推測で
  全面的に書き換えない。** 変更する場合は実データで検証してから。
- **`is_complete`フラグや係数を手動でTrue/改ざんしない。**
- **負のEVを積極的に買う方向への変更はしない**(既に再計算で負は正当と確認済み)。
- **選択ロジックの閾値(EV最大化 vs 的中率重視など)を勝手に変えない。**
  設計合意してから。
- git操作(push)はユーザーが実行する。Claude/Grokはzipまたはパッチで返す。
- **git commitとgit pushのコマンドは、実行状態を考慮して書くこと。**
  - `git commit`が既に成功している場合、次のコマンドは`git push origin main`だけを書く。
  - `git commit && git push`を、既にコミット済みの状態で再実行しない。
    `git commit`が「nothing to commit」で終了コード1になると`&&`により`git push`まで実行されないため。
  - 修正→commit→pushを一度に提示する場合は、commit対象を明示し、commit成功後にpushまで実行される構成にする。
  - ユーザーがcommit済みの実行結果を提示した場合は、commitを再実行せず、pushだけを提示する。
- 既存実装を確認した上で最小差分。推測での大規模改修はしない。
- **投票ロジック(予想確率・確率補正・運用ゲート等)を修正したら、
  `app/routers/purchases.py`の`CALIBRATION_SWITCH_AT`を必ずその日の日付に
  更新すること。** 集計・診断系エンドポイントは既定でこの日時以降だけを
  「現行基準」として集計するため、更新を忘れると新旧ロジック混在データが
  現行基準として表示されてしまう(2026-09-06にのんの指摘で発覚)。
- デプロイコマンドは実際に動く形で書く(プレースホルダー禁止)。
  リポジトリのローカルパスは `~/Keirin-EV-Tool`、zipのダウンロード先は
  `~/storage/downloads`。
  型:
  ```bash
  cd ~/Keirin-EV-Tool && \
  unzip -o ~/storage/downloads/<zip名> -d . && \
  git add <変更ファイル> && \
  git commit -m "<内容>" && \
  git push && \
  rm -f ~/storage/downloads/<今回のzip>
  ```
- **`git add -A` / `git add .` は絶対に使わない。** 必ず今回変更したファイルだけを
  明示的に列挙して`git add`する。
- **commit対象を提示する前に、必ず`git status --short`の出力を1行ずつ確認する。**
  以下に該当する行が1つでもあれば、その行は`git add`の対象から除外する(気づいた場合は
  ユーザーに一言注意喚起する)。
  - `__pycache__/`、`*.pyc`
  - `*.bak`、`*.bak_*`などのバックアップファイル
  - `scraper/data/`配下のログ・replay・joblist等の実行時生成物
  - `160000`表示や「embedded git repository」警告が出るサブディレクトリ(埋め込みリポジトリ)
  - その他、今回の修正と無関係なファイル
  (2026-09-07: `git add -A`を提示した結果、上記が全て誤ってコミットされる事故が発生したため
  ルール化)。
- コマンドはできるだけ1行・スクリプト化。bankrollは検証時1000000を明示。
- 実資金投票は精度が目安に届くまで開始しない
  (investment-readinessの4基準: サンプル数・統計的有意性・実績収支率黒字・
  破産確率10%以下)。
- スクリプト実行コマンド(replay_settled.py等)を提示する際は、必ず
  `cd ~/Keirin-EV-Tool &&` を先頭に明記する(単体で書くとFileNotFoundになる)。
- 重複していた「追加分析課題」一覧(2系統)・2026-09-09のChatGPT引き継ぎメモは
  `PROGRESS_ARCHIVE.md`末尾に退避済み(2026-09-10整理)。過去の経緯を確認したい
  場合はそちらを参照する。現在の課題は上記「1. 現在の状況」の「残作業」のみ。

## since時刻運用ルール（2026-09-13）
- `since` の基準時刻は必ず**秒単位のUTC絶対時刻**で記録する。
- 暦日だけ、JST 00:00、日付から推測した時刻を `since` の基準にしない。
- `VOTING_CRITERIA_UPDATED_AT` は最後に投票判断そのものを変更した時刻を記録する。
- `since=calibration_switch` は `VOTING_CRITERIA_UPDATED_AT` を使用する。
- タイムゾーン付きISO日時はUTCへ正規化してから比較する。
- 過去レースの再投票では `Purchase.purchased_at` は再投票実行時刻になるため、レース開催日時と混同しない。
- 今回の投票基準変更の記録時刻は、変更コミット `a592f0977a9bae1aad152b67bc8c015cf5beda8b` のコミット時刻
  `2026-09-12 15:21:47 UTC`（JST `2026-09-13 00:21:47`）を基準とする。
- 今回の9/12未確定レースは既に結果確定済みであり、その後の全過去レース再投票結果を現行モデルの検証対象とする。
- 9/12のうち今回の未確定39レースに含まれない既確定レースだけを、再投票漏れ候補として別途確認する。
## デグレ防止：収益記録の想定利益計算

- **発生原因**：実投資額と予定投資額を分離して管理する仕様であるにもかかわらず、修正前に実際の値の取得元・計算元・API送信値・バックエンド保存処理まで一連の経路を確認せず、既存仕様を満たしているフロント側の計算とは別に、バックエンドの再計算処理だけを前提として原因を判断した。
- **本質的な原因**：値の取得元から保存先までのデータフローを確認せずに修正箇所を決めたこと。実投資額(`actual_stake`)と予定投資額(`planned_stake`)という異なる意味の値について、どの処理が最終値を決定するかを変更前に固定していなかった。
- **対策**：今後の修正では、必ず「入力値の取得元 → 計算元 → API payload → バックエンド受信 → DB保存 → 表示」の実経路を先に確認し、その経路上で最終値を決定している箇所を特定してから修正する。フロントとバックエンドの双方に同じ値の計算が存在する場合は、サーバー側で仕様上の不変条件を保証する。
- **今回の不変条件**：`planned_stake` は投票プラン上の予定投資額、`actual_stake` は実際の投資額。`planned_expected_profit` は `actual_stake × (planned_win_prob × planned_odds − 1)` とする。
- **回帰ケース**：予定投資6000円、実投資100円、的中率3.4%、オッズ100倍の場合、想定利益は14,400円ではなく240円でなければならない。

## デグレ防止：検証完了前のコミット・プッシュ禁止

- **作業原因**：検証工程とコミット・プッシュ工程を明確に分離せず、検証が成功した場合だけ後続工程へ進むという作業フローを保証していなかった。
- **対策**：今後は「検証成功」をコミット・プッシュの必須前提条件とする。検証が1つでも失敗した場合は、`git add`・`commit`・`push` を実行せず、その時点で作業を停止する。
- **運用ルール**：検証 → 成功確認 → `git add` → `commit` → `push` → push後確認の順序を固定し、検証未完了の変更を確定・公開しない。
- **最優先事項**：デグレ防止と同様、検証工程を省略・迂回せず、検証成功をコミット・プッシュの絶対条件とする。

## デグレ防止：作業を途中で停止しない運用

- **作業原因**：修正・検証・確定・公開を一連の作業として最後まで完了させるという運用を徹底せず、途中工程で作業が停止する状態を許容した。
- **対策**：「修正して」「進めて」と指定された作業では、現状確認 → 原因特定 → 最小修正 → 検証 → commit → push → push後確認までを連続して実施し、次にユーザーの操作・判断が必要になる地点まで途中で停止しない。
- **停止条件**：対象ソースが想定と一致しない、検証対象が特定できない、または修正によるデグレの可能性が解消できない場合のみ、commit・pushを行わず停止する。
- **検証方法**：数値計算の回帰検証では浮動小数点の完全一致比較を避け、仕様上の許容誤差を用いて判定する。

### 作業継続・改行混入防止
- Keirin-EV-Toolの作業開始時は必ずPROGRESS.mdの最優先作業ルールを確認する。
- 「進め」「続行」「修正して」等の指示では、ユーザーの操作・認証・判断が必要になる地点まで途中で停止しない。
- 現状確認→原因特定→最小修正→構文検証→回帰検証→commit→push→push後確認を一連の作業として扱う。
- 長大な多段shellコマンドや複数heredocの連結による貼り付け時の改行・空白混入を避ける。今後は原則として1つの実行ブロックにまとめ、対象文字列の完全一致だけに依存しない。
- 修正前に取得元・計算元・送信元・受信側・保存先を確認し、修正対象外の未コミット変更を巻き込まない。
